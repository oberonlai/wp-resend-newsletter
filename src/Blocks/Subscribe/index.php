<?php
/**
 * Block package entry (everything-wp /frontend-page layout).
 *
 * Registration is performed by {@see SubscribeBlock::register()} from Bootstrap.
 *
 * @package WpResendNewsletter
 */

declare(strict_types=1);

// Intentionally empty — PSR-4 loads SubscribeBlock.php; Bootstrap calls register().
